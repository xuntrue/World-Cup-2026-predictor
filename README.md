# World-Cup-2026-predictor
Using the power of ML to predict who's going to win the world cup.

- `src/` — My models + simulation code
- `data/` — files generated or preprocess for your project (you don’t commit large raw copies)
- `notebooks/` — documentation
- `configs/` — model/simulation configuration
- `results/` — outputs (probabilities, bracket simulations, etc.)
- `vendor/` — upstream dependencies you pulled in (the submodule)